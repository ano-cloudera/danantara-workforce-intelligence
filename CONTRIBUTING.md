# Contributing

1. Pick or create a story under `_bmad/stories`.
2. Record acceptance criteria before coding.
3. Run `python scripts/preflight.py`.
4. Run backend tests from `apps/backend`.
5. Keep `.env.example` synchronized with new configuration.
6. Update docs when deployment steps change.
7. Never commit credentials, customer CVs or policy documents.
