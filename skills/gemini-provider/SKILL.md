# Skill: gemini-provider

Use Gemini consistently through the Google Gen AI SDK.

## Rules
- Import `from google import genai`.
- Support API key and Vertex AI modes via config.
- Keep text and embedding model names configurable.
- Request a configurable embedding dimensionality.
- Never log API keys or full sensitive prompts.
