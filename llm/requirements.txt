ray[default,serve]==2.48.0
vllm==0.9.2
torch==2.7.0
transformers==4.51.3
tokenizers>=0.19,<0.22
httpx==0.28.1
huggingface-hub>=0.34,<1
autoawq==0.2.9
# fastapi must satisfy both: >=0.115.0 (vllm's own requirement) and
# <0.139.2 (that release added a threading.Lock to route caching that Ray
# Serve's @serve.ingress cannot pickle, ray-project/ray#64939).
# starlette==0.40.0 is the floor fastapi==0.128.0 declares (>=0.40.0,<0.51.0).
fastapi==0.128.0
starlette==0.40.0
# ray==2.48.0 declares protobuf!=3.19.5,>=3.15.3, a bound loose enough to
# allow protobuf 6.x, which dropped FieldDescriptor.label and breaks Ray
# Serve's _proto_to_dict (ray-project/ray#54849).
protobuf==4.25.6
