# NiFi demo data

This directory is reserved for files owned by the NiFi team's ingestion-to-Qdrant demo.
Do not use it for Workforce Intelligence application state, uploads, or demo fixtures.

NiFi indexes its documents into the collection configured by `QDRANT_NIFI_COLLECTION`.
